// If the first compilation (whatever the code) works, 
// the whole thing works.
// E.g., compile line 4 after commenting out lines 9-15, and
val foo = "bar" // then, uncomment and re-compile

// import collection.Seq
// val x = Seq(20)
/* */
def pic = 20
def makePic(s: Int = {
    pic
}

val x = Seq(10)
println(x)
/* */